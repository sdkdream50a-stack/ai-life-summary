# Cloudflare Pages redirect rules — INERT on GitHub Pages (current host).
# This file is preserved as documentation for potential CF Pages migration.
# Live host (GH Pages legacy build) ignores _redirects entirely.
# Sensitive files are excluded via .gitignore + .nojekyll instead.

# terms-of-service -> terms (canonical)
/terms-of-service /terms 301
/terms-of-service.html /terms 301

# Language-specific pages -> canonical pages
# (Removed /ja/contact/ /en/contact/ /en/terms-of-service/ legacy redirects —
#  localized index.html files exist and are the canonical hreflang targets in sitemap.)
/es/guide/compatibility-guide/ /blog.html 301
/zh/methodology/ /blog.html 301

# Search template URL
/search / 301

# Root entry (thin JS-redirect loaders) -> English canonical for AdSense crawler
/age-calculator/ /en/age-calculator/ 301
/compatibility/ /en/compatibility/ 301
/life-summary/ /en/life-summary/ 301
/personality-type/ /en/personality-type/ 301
/vibe-check/ /en/vibe-check/ 301
/kpop-match/ /en/kpop-match/ 301
